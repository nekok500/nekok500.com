---
import Prose from '../components/Prose.astro'
import BaseLayout from './BaseLayout.astro'

const { title, heading } = Astro.props
---

<BaseLayout pageTitle={title} heading={heading}>
  <Prose>
    <slot />
  </Prose>
</BaseLayout>
